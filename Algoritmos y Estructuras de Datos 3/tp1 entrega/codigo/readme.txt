para compilar yo use el clion, no deberia haber ninguna complicacion, solo se necesita el main.cpp
el codigo trae en si los 4 algoritmos, 
para poder compilarlo y que el output e input sea como piden en el enunciado
es necesario que :
en el main
se cumpla bool entrega = true  
en el main hay una variable llamada cual que te permite elegir que algoritmo correr
cual=0 (fuerza bruta), cual=1 (meet in the middle), cual=2 (backtracking), cual=3 (programacion dinamica)
respetando lo de arriba el programa deberia correr tal cual piden en el formato de input output.

extra: si quieren por ejemplo no tener que compilar denuevo el codigo con la variable "cual" en distinto numero,
pueden des-comentar las lineas 528 529 530 531 (son las primeras 2 lineas en la funcion main()   y comentar la linea 543 
para asi poder en consola decirle que algoritmo quieren usar
sino la otra opcion es copiar 4 veces el mismo codigo y compilarlo las 4 veces con la variable cual en diferente numero
(como no sabia que opcion preferian dejo todas a su dispocición)